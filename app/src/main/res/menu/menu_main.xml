<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/menu_main_action_bar"
    >
    <!-- Note: action bar actions are implemented in MainActivity -->
    <item
        android:id="@+id/action_bar__help"
        android:orderInCategory="100"
        android:title="@string/action_bar__help"
        android:icon="@drawable/ic_help_icon"
        app:showAsAction="ifRoom"
        />
    <item
        android:id="@+id/action_bar__about"
        android:orderInCategory="200"
        android:title="@string/action_bar__about"
        android:icon="@drawable/ic_about_icon"
        app:showAsAction="ifRoom"
        />
</menu>
